{
    "name": "nodejs-apollo-server",
    "description": "An AWS Lambda integration of GraphQL Server.",
    "version": "0.0.1",
    "private": false,
    "dependencies": {},
    "devDependencies": {
        "aws-sdk": "^2.815.0",
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest --roots __tests__/unit",
        "integ-test": "jest --roots __tests__/integration"
    }
}